# 당일 이슈 백로그 — 18개 (200줄 이하 단위)

> 아침에 `bash scripts/seed-issues.sh <주영> <승빈> <연성>` 한 번으로 일괄 생성.
> 아이디어 정의는 [IDEA.md](IDEA.md).
> **층 구조**: B·C가 함수를 만들고 → D가 그 함수를 호출하는 오케스트레이터를 만들고 → E가 화면을 붙인다.
> 순서 = 의존 순서. A가 끝나야 B가 돌고, B·C가 끝나야 D가 의미 있다.
> assign: 인프라·화면→주영, 계산 함수→승빈, AI 서술→연성. **연성은 발표 고정이라 구현 몫을 줄였다.**

## A. 인프라·배포 (3) — 주영

1. **[A1] 프로젝트 뼈대 + smoke.sh 데모 시나리오**
   스택 초기화, `/` 가 뜨면 통과하도록 `scripts/smoke.sh` TODO 채움 (사람이 채우는 유일한 예외 커밋). 완료 조건: `bash scripts/smoke.sh` → SMOKE OK
2. **[A2] Vercel 연결 — main 자동 배포**
   main 머지마다 자동 배포. 완료 조건: 배포 URL 접속 시 A1 화면. **제출 링크는 별칭 도메인** (배포 URL은 익명 접속이 로그인 벽에 막힌다)
3. **[A3] 데이터 수집·정규화**
   시간표(15042241, cp949)·화물역(15042207)·운행횟수(15068417/20) + 열차수송통계 API. 키는 `.env`, 커밋 금지. 완료 조건: `public/data/*.json` 생성

## B. 데이터 함수 (5) — 승빈

> 전부 **순수 함수**로 만든다. LLM이 호출할 도구가 되므로 입력·출력 형태를 먼저 고정한다.

4. **[B1] 시퀀스 복원 + 정차사유별 체류시간**
   열차번호 단독 키로 정렬(복합키 불필요), 자정 넘김 `% 1440`. 완료 조건: **화물취급 5,094분 · 대피 1,733분 재현**
5. **[B2] X-factor 산출**
   총 소요 ÷ 순수 주행. **분모 정의는 오전에 확정**(권장: 총소요 − 정차합). 완료 조건: **전체 평균 약 1.3** 재현. B1 의존
6. **[B3] OD 물량·톤키로 조회**
   출발역·도착역·품목으로 실적 물량과 `ftsp_dtkm` 조회. 완료 조건: 샘플 3개 OD 수작업 대조 일치. A3 의존
7. **[B4] 편방향 판정**
   역↔역 양방향/편방향 분류. 완료 조건: **편방향 249쌍(63.5%) 재현**. B3 의존
8. **[B5] 복화 가능성 필터**
   편방향 구간의 **도착역에서 같은 품목이 나가는지** 교차 대조. 완료 조건: **77건·208,989톤(7.0%) 재현**. **v2의 핵심 발견이다.** B4 의존

## C. 편익 계산 함수 (2) — 승빈

9. **[C1] 환경 편익 — 탄소·대기오염**
   철도/도로 원단위를 곱해 배출량 차이 산출. 완료 조건: 원단위 값과 계산식이 응답에 포함됨. **⚠️ 원단위 출처는 사람이 먼저 확정** (맨 아래 참고)
10. **[C2] 사회 편익 — 교통사고·도로혼잡**
    전환 물량을 트럭 대수로 환산 → 사고·혼잡 감소 추정. 완료 조건: 트럭 환산 근거가 응답에 포함됨

## D. 오케스트레이션 (4) — AI가 핵심으로 작동하는 자리

11. **[D1] 도구 스키마 + 라우팅** — 주영
    B·C 함수를 LLM 도구로 등록하고, 질문에 따라 호출을 결정하게 한다. **숫자 계산을 LLM에 시키지 않는다.** 완료 조건: "구미에서 부산 컨테이너 주 3회"가 B3→C1→C2→B4 순으로 호출됨
12. **[D2] 지식 베이스 — 데이터 한계** — 승빈
    IDEA.md의 한계 목록을 섹션 태그로 정리해 답변 시 주입. **벡터DB 쓰지 않는다** (문서가 작아 오히려 부정확해진다). 완료 조건: 부산신항 관련 질문에 "시간표 데이터에 없음" 경고가 붙음
13. **[D3] 근거 인용 서술 생성** — 연성
    계산 결과 + 한계를 묶어 **화주용 / 정책용**으로 다르게 서술. 근거 수치 인용 필수. 완료 조건: 두 서술이 같은 숫자를 쓰되 논지가 다름
14. **[D4] 범위 밖 질문 처리** — 연성
    함수로 답할 수 없는 질문은 **지어내지 말고 "저희 데이터 밖입니다"로 떨어뜨린다.** 완료 조건: 미리 정한 범위 밖 질문 3개가 전부 정직하게 거절됨

## E. 화면 (2) — 주영

15. **[E1] 대화 화면**
    질문 입력 → 답변 + **호출된 함수 표시**. 어떤 계산을 거쳤는지 사용자에게 보이게 한다
16. **[E2] 근거 카드 + 되먹임**
    답변 아래 편익·시간·방향 카드. **서술 속 구간 이름 클릭 → 그 구간으로 카드 갱신** *(제9조 "단순 출력 나열" 방어 장면)*

## F. 마무리 (2)

17. **[F1] 시연 영상 촬영·제출물 정리** — 연성. 접속 링크(별칭 도메인) + GitHub 링크 + 영상
18. **[F2] README** — 주영. 한 줄 소개, 실행법, 데이터 출처, **원단위 출처**

## 생성하지 않는 것 (스코프 방어)

운송 신청·예약 / 실시간 열차 추적 / 배차 / 3사 연동 / KTDB 원본 업로드 / 벡터DB — 발표에서 말로만.

## ⚠️ 사람이 할 일 (루프에 맡기지 않는다)

- **C1·C2의 편익 원단위 출처 확정.** 숫자를 지어내면 발표에서 무너진다. **공식 통계만.** 오전 1순위
- **데모 질문 확정** — 답할 질문 3~4개, 일부러 거절할 질문 3개. 자유 질의는 심사장에서 무너진다
- `scripts/smoke.sh` · `.claude/commands/hack.md` 수정
