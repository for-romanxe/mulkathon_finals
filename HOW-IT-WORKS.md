# 작업 자동화 방식 설명 — 자율 개발 루프

> 한 문장: **셋의 노트북에서 각자 Claude가 1분마다 깨어나 GitHub을 보고, 사다리에서 처음 걸리는 일 하나만 하고 다시 잠드는 구조.**
> 팀원 설명용 비유: "1분마다 출근해서 게시판 확인하고, 딱 한 가지 일만 하고 퇴근하는 알바 셋"

---

## 1. 구조

```
연성 노트북: Claude 루프 ─┐
주영 노트북: Claude 루프 ─┼──▶ GitHub (이슈·PR·리뷰) ◀── 사람: 이슈만 공급
승빈 노트북: Claude 루프 ─┘        ↑ 유일한 소통 창구
```

핵심 설계: **루프끼리 직접 대화하지 않는다.** 모든 소통은 GitHub의 이슈·PR·리뷰·라벨을 통해서만.

- 누가 언제 켜고 끄든, 노트북이 죽든 서로 영향 없음
- GitHub이 유일한 진실 — 각 틱은 "지금 GitHub 상태"만 보고 판단 (이전 틱 기억 불필요)
- 개발 용어로는: GitHub 중심의 폴링 기반 멀티 에이전트 오케스트레이션

## 2. 한 틱에서 일어나는 일

`/loop 1m /hack 아이디` 실행 후 1분마다:

1. **폴링** — 열린 PR·내게 assign된 이슈 목록 조회
2. **사다리** — 위에서부터 검사, **처음 걸리는 것 하나만** 실행
3. **보고** — 3~4줄 남기고 종료 → 1분 뒤 다시 1번부터

| 순위 | 조건 | 행동 |
|---|---|---|
| 1 | 내 PR에 수정 요청이 달림 | 고쳐서 다시 푸시 |
| 2 | 내 PR이 승인 + 통과 상태 | 머지 → main 스모크 |
| 3 | 남의 PR에 내 리뷰(재리뷰) 필요 | approve / request-changes 중 하나로 반드시 판정 |
| 4 | 내게 assign된 이슈 있음 | 브랜치 → 구현 → PR |
| 5 | 아무것도 없음 | main 안 깨졌나 확인만 하고 대기 |

## 3. 왜 이 순서인가

**"끝내는 일"이 위, "새로 벌이는 일"이 아래.**

- 순서를 뒤집으면 → 셋 다 4단계(새 브랜치)만 반복 → 브랜치 10개, 머지 0개
- 이 순서면 → 벌어진 일(리뷰 대기·머지 대기)이 항상 먼저 소화 → 작업이 흐른다

정상 흐름 예시:
```
내 루프: 이슈 집어 PR 생성 (4단계)
  → 1~2분 내 남의 루프: 리뷰 (3단계)
    → 다음 틱 내 루프: 머지 (2단계)
      → Vercel: main 자동 배포
사람은 그동안 회의만.
```

## 4. 사람의 역할 — 딱 둘

| 역할 | 내용 |
|---|---|
| **이슈 공급** | 루프는 이슈에 적힌 것만 한다. 아침에 `bash scripts/seed-issues.sh`로 백로그 18개 일괄 투입, 이후 회의에서 나오는 것 추가 |
| **예외 처리** | 뭔가 걸리면 수동 개입 (아래 탈출구) |

## 5. 폭주 방지 장치

| 장치 | 강제 주체 |
|---|---|
| 새 기능 스스로 발명 금지 (이슈에 있는 것만) | 프롬프트 규칙 |
| 셀프 approve 금지 | **GitHub이 원천 차단** |
| approve 없는 머지 금지 | **브랜치 보호가 차단** |
| main 직접 푸시 금지 | **브랜치 보호가 차단** |
| 200줄 초과 시 스스로 중단 + "쪼개자" 코멘트 | 프롬프트 규칙 |
| 2회 연속 실패 시 help-needed 넘기고 다른 일 | 프롬프트 규칙 |

→ 루프가 어떤 틱에서 잘못 판단해도 **최악은 "PR 하나 반려"**, main은 안 깨진다.

## 6. 막혔을 때 탈출구

루프는 자동화지 감옥이 아니다. 이상하면 언제든 사람이 개입:

```bash
gh pr review <n> --approve            # 사람이 직접 리뷰
gh pr merge <n> --squash --admin      # owner 권한 강제 머지 (enforce_admins:false라 가능)
```

## 7. 검증 상태 (정직하게)

| 항목 | 상태 |
|---|---|
| reviewDecision 함정 + 브랜치 보호 해법 | ✅ 실제 GitHub에서 검증 |
| 셀프 approve 차단 · approve 없는 머지 차단 | ✅ 검증 |
| request-changes 데드락 수정 (재리뷰 조건) | 🟡 규칙은 수정, 실전 미구동 |
| 팀원 approve → 머지 성공 | 🟡 계정 2개 필요, 미검증 |
| 루프 3개 동시 구동 | 🟡 미구동 |

→ 시작 직후 **첫 PR이 리뷰→머지 한 바퀴 도는지 30분 안에 확인**. 돌면 믿고 회의 집중, 안 돌면 6번 탈출구로 수동 전환.

## 8. 파일 구성

| 파일 | 역할 |
|---|---|
| `.claude/commands/hack.md` | 루프 규칙 (사다리·가드레일) — `/hack` 명령 정의 |
| `scripts/smoke.sh` | "main이 데모 되나" 공용 판정기 (TODO 한 줄은 당일 사람이 채움) |
| `scripts/seed-issues.sh` | 백로그 18개 일괄 생성 |
| `BACKLOG.md` | 이슈 목록 원본 (의존 순서 포함) |
| `SETUP.md` | 대회장 세팅 순서 (담당: 주영) |
