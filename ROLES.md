# 역할 분담 — 누가 무엇을 건드리는가

> 2026-08-13 10:50 작성. **중복 작업이 세 번 나온 뒤** 만들었다.
> 이슈 담당표만으로는 충돌이 안 막혔다. 이 문서는 **파일 소유권**으로 막는다.

## 왜 만들었나 — 오늘 실제로 터진 것

| 시각 | 무슨 일 | 원인 |
|---|---|---|
| 10:16 | 이슈 18개가 33개로 생성됨 | 연성과 주영이 `seed-issues.sh` 를 각자 한 번씩 실행 |
| 10:26 | `hack.md` 200줄 예외를 둘이 동시에 작성 (PR#54 폐기) | 연성이 main 직push, 주영이 PR — 같은 파일 |
| 10:46 | **A3 정규화 파이프라인을 둘이 각자 구현** (PR#55·#56) | 연성이 주영 담당 이슈를 선점 절차 없이 가져감 |

공통 원인은 하나다. **"이 파일은 누구 것인가"가 어디에도 없었다.**

---

## 1. 파일 소유권 — 이게 본체다

PR을 올리기 전에 `git diff --name-only` 를 보고, **내 소유가 아닌 경로가 있으면 그 사람에게 넘긴다.**

| 경로 | 소유 | 내용 |
|---|---|---|
| `src/app/**` | **주영** | 화면·레이아웃·라우트 (E1·E2) |
| `src/lib/tools/**` | **주영** | LLM 도구 스키마·라우팅 (D1) |
| `next.config.ts` `package.json` `tsconfig.json` 배포 설정 | **주영** | 인프라 (A1·A2) |
| `scripts/normalize.py` `data/**` | **주영** | 원본 수집·정규화 (A3) |
| `src/lib/calc/**` | **승빈** | 순수 계산 함수 — 체류시간·X-factor·OD·편방향·복화·편익 (B1~B5·C1·C2) |
| `src/lib/knowledge/**` | **승빈** | 데이터 한계 지식 베이스 (D2) |
| `src/lib/narrate/**` | **연성** | 화주용·정책용 서술, 범위 밖 질문 처리 (D3·D4) |
| `README.md` | **주영** | (F2) |
| `IDEA.md` `ROLES.md` `BACKLOG.md` `.claude/commands/hack.md` `scripts/smoke.sh` | **사람만** | 루프 수정 금지. 바꾸려면 사람이 직접 |
| `public/data/**` | **생성물** | 손으로 고치지 않는다. `scripts/normalize.py` 로만 생산 |

**경계에 걸치는 것 둘**

- **`src/lib/calc/` 의 함수 시그니처**는 승빈이 정한다. 주영은 D1에서 그 시그니처를 **읽어서** 도구로 등록만 한다. 시그니처를 바꿔야 하면 승빈에게 이슈로 요청한다.
- **프롬프트 문자열**은 연성 것이다. 주영이 D1에서 라우팅 프롬프트가 필요하면 `src/lib/narrate/` 에서 import 한다.

## 2. 이슈 담당표

| | 주영 `Imtylerrrrrr` | 승빈 `sonamoo0407` | 연성 `for-romanxe` |
|---|---|---|---|
| **역할** | 인프라 · 화면 · 라우팅 | 계산 함수 · 지식 베이스 | AI 서술 · 제출물 · 발표 |
| **이슈** | A1 A2 A3 · D1 · E1 E2 · F2 | B1~B5 · C1 C2 · D2 | D3 D4 · F1 |
| **개수** | 8 | 8 | 3 |

연성이 3개인 것은 **발표 준비가 고정 업무**이기 때문이다. 여유가 아니다.

## 3. 남의 담당 이슈를 가져가야 할 때 — 절차 (사람만)

의존이 막혀서 남의 이슈를 대신 해야 할 상황이 실제로 생긴다. 그때는 **반드시 이 순서로**:

```bash
gh issue view <n> --json assignees,labels    # 1. 이미 in-progress면 절대 손대지 않는다
gh issue edit <n> --add-assignee <나> --remove-assignee <원담당>
gh issue edit <n> --add-label in-progress
gh issue comment <n> --body "제가 가져갑니다: <이유>. 원담당 <이름>은 <대신 할 것>으로 넘어가 주세요"
```

**assign을 옮기지 않고 라벨만 붙이면 막지 못한다.** 오늘 A3이 그렇게 중복됐다 — 라벨은 붙였지만 담당이 주영으로 남아 있어서 주영 루프가 그대로 집었다.

가져가기 전에 **원담당에게 말로 먼저 알린다.** 루프는 코멘트를 읽지만, 이미 착수한 틱은 되돌릴 수 없다.

## 4. 의존이 막혔을 때 — `help-needed` 를 쓰지 않는다

`help-needed` 는 **내가 시도했는데 실패한 것**에 붙인다. 선행 이슈가 안 끝나서 시작조차 못 하는 것은 실패가 아니다.

오늘 B1·B2가 이 구분이 없어서 담당자까지 해제된 채 주차됐다. 그렇게 되면 선행이 끝나도 **아무 루프도 다시 집지 않는다.**

- 선행 미완으로 막히면 → **코멘트 한 줄만 남기고 그 이슈는 건너뛴다.** 라벨·담당 건드리지 않는다
- 다음 순위 이슈로 내려간다. 내 담당 전부가 막혀 있으면 5단계(대기)로 간다
- 같은 이슈에 이미 "선행 대기" 코멘트가 있으면 **다시 달지 않는다**

## 5. 사람이 지켜야 할 것 셋

1. **`seed-issues.sh` 는 한 번만.** 실행 전 `gh issue list` 로 이미 있는지 본다
2. **`hack.md` `smoke.sh` `ROLES.md` 를 고치기 전에 브랜치·PR 상태를 확인한다.** 남이 같은 파일로 PR을 열어놨을 수 있다
3. **main 직push는 프로세스 문서에만.** 제품 코드는 예외 없이 PR을 거친다
