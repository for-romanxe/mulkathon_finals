# 제출물 — 철도 전환 편익 오케스트레이터

심사위원에게 내보내는 것 3개. **링크 2개는 확인 완료, 영상은 사람이 찍어야 한다.**

## 1. 접속 링크

<https://rail-console.vercel.app>

- 2026-08-13 16:20 배포본 확인 — **편익 금액이 실제로 나온다** (도로 170.6억 → 철도 58.6억 → 편익 112억)
- **15:47 주영이 자기 Vercel 계정으로 프로젝트를 옮겼다.** 옛 주소(`rail-console-for-romanxe1`)는 연성 계정이고
  일일 배포 한도에 걸려 더 못 올린다. **이 주소가 제출 링크다.**
- ⚠️ **자동 배포가 안 걸려 있다.** 새 프로젝트에 GitHub 연동이 없어서, #123·#125를 머지해도 배포가 생기지 않았다.
  **머지한 사람이 `npx vercel --prod --yes` 로 직접 올려야 한다.** 주영이 대시보드에서 레포를 연결하면 풀린다

⚠️ **주소 주의** — `rail-console.vercel.app` 은 이전에 A1 플레이스홀더를 서빙하던 주소였다(#70).
지금은 주영 계정이 인수해 **본 프로젝트를 서빙한다.** 옛 문서·메모에 "다른 프로젝트"라고 적힌 것은 무효다.

| | 주소 | 상태 |
|---|---|---|
| **제출 링크** | `rail-console.vercel.app` | 주영 계정 · 배포 가능 · 최신 main |
| 옛 주소 | `rail-console-for-romanxe1.vercel.app` | 연성 계정 · 배포 한도 초과 · 같은 버전이나 갱신 불가 |

## 2. 소스 코드

<https://github.com/for-romanxe/mulkathon_finals>

- Public 확인 완료 — 로그인 없이 열린다
- 개발 방식 설명은 `HOW-IT-WORKS.md`, 실행법은 `README.md`(#53)

## 3. 시연 영상 — ⚠️ 사람이 할 일

루프는 화면 녹화를 할 수 없다. 아래는 **찍을 것만 정리한 것**이고, 촬영·편집·업로드는 사람이 한다.

> 2026-08-13 14:57 브라우저로 배포본을 직접 열어 전 경로를 확인하고 다시 썼다.
> 화면이 챗봇에서 **콘솔 대시보드**로 바뀌었다(#96) — 이전 대본은 더 이상 맞지 않는다.

### 첫 화면 — 비어 있는 것이 의도다

> 🔴 **16:24 #127이 머지되면서 첫 화면이 바뀌었다.** 경고 배너와 KPI 카드는 **없어졌다.**
> 아래는 `src/app/page.tsx` 를 직접 읽고 다시 쓴 것이다(배포본 확인은 아직 못 했다 — 촬영 전 확인 참고).

화면은 3단이고, 시작할 때는 **왼쪽 상담 기록 + 가운데 대화** 둘만 보인다. 우측 근거 패널은 닫혀 있다가
**답이 오면 밀려 나온다.** 아무것도 안 누른 상태에서 보이는 건 이것뿐이다:

```
좌측    전환 편익 콘솔 · 상담 기록 · 코레일 물류 영업 / 화주 미팅용

가운데  화물을 철도로 옮기면 무엇이 나아질까요?
        구간을 말씀해 주시면 실제 운송 실적으로 물량·거리·회송 부담을 계산해 드립니다.
        숫자는 코드가 계산하고, 근거가 없으면 없다고 답합니다.

        → 오봉에서 북철송장으로 컨테이너 보내는데, 철도로 바꾸면 뭐가 좋은가요? …
        → 신광양항에서 군산항 구간은 복화가 가능한가요?
        → 구미에서 부산으로 보내는 물량은요?
```

**부제 두 줄이 이 제품의 주장 전체다** — "숫자는 코드가 계산하고, 근거가 없으면 없다고 답합니다".
여기서 3초 멈추고 읽어주면 뒤의 세 컷이 전부 그 문장의 증명이 된다.

전국 집계 수치(철도로 옮긴 화물·빈 차로 돌아오는 구간 비중·소요시간 배수·정차 분)는
**우측 패널의 `계산 단계` 안**으로 들어갔다. 질문 전에 보여주려면 우측 패널을 한 번 열어야 한다.

### 찍을 장면 — 3컷

이 제품의 주장은 "숫자는 코드가, 해석은 AI가 — 환각 0"이다. 그 주장은 **답한 장면 하나로는 증명되지 않는다.**
모르는 걸 모른다고 하는 장면이 같이 있어야 증명된다.

**① 긍정 경로** — 제안 질문 첫 번째를 클릭

```
오봉에서 북철송장으로 컨테이너 보내는데, 철도로 바꾸면 뭐가 좋은가요? 돌아오는 화물도 있나요?
```

화면에서 짚을 것:

- **함수 호출 배지 5개** — `구간 물량 조회 · 돌아오는 화물 확인 · 돌아올 때 실을 화물 찾기 · 탄소·대기오염 절감 계산 · 교통사고·혼잡 절감 계산`
  (#122로 한국어가 됐다. 영문 코드명이 보이면 옛 배포본이다)
- **답변 수치가 배지의 도구 반환값과 같다** — 139일 실적 기준 377,337.5톤 · 평균거리 408.1km · 역방향 247,945톤(39.7%)
- **근거 카드 3장** — 물량·거리 / 방향 / 편익
- 🔴 **편익 카드에 금액이 찍힌다** — 16:15부터 바뀐 부분이다. `도로로 보낼 때 17,059,171,036원` / `철도로 보낼 때 5,856,294,227원` / `절감액 11,202,876,809원`.
  출처 줄에 **「2024년도 전환교통지원사업 협약사업자 공모 설명자료」** 가 그대로 붙는다 — 국토부 보조금 산정식에 실제로 쓰이는 값이다
- **같은 답변 안에서 탄소는 아직 "기준값 적용 후 알려드리겠다"고 말한다** — 근거 있는 건 말하고 없는 건 안 말하는 게 한 화면에 같이 보인다.
  ⚠️ **다만 이 통합 원단위에는 대기오염·온실가스가 이미 포함돼 있다.** 심사위원이 "탄소는 별도냐"고 물으면 **"112억에 이미 포함"** 이라고 답해야 한다
- 🔴 **지도에서 오봉→북철송장이 파랗게 강조되고 나머지가 회색으로 죽는다** — 되먹임 장면. 규정 제9조 "단순 출력 나열" 방어

**② 한계 고지** — 직접 입력

```
부산신항에서 오봉으로 보내면 소요시간이 어떻게 되나요?
```

- **도구를 하나도 부르지 않고** 답한다
- *"이 구간은 시간표 데이터에 없어 소요시간 진단은 드릴 수 없습니다.
  전 노선 평균치를 이 구간의 값처럼 말씀드릴 수는 없습니다."*
- **전 노선 평균을 갖다 붙이지 않는 것**이 핵심이다. 지식 베이스(#101)가 질문에 걸리는 한계를 자동으로 붙인다

**③ 범위 밖 거절** — 직접 입력

```
오봉에서 부산신항으로 화차 예약할 수 있나요?
```

- **도구 0회 호출**, 즉시 거절
- *"운송 신청·예약·배차는 저희가 만들지 않았습니다. 코레일이 구축 중인 플랫폼의 몫이고,
  저희는 그 위에 얹는 진단 계층입니다.
  그 구간의 물량·거리·편방향 여건과 전환 편익은 지금 바로 알려드릴 수 있습니다."*
- 거절만 하지 않고 **대신 할 수 있는 것**을 말한다. 예상 Q&A의 "플랫폼은 우리가 구축 중"에 대한 답이 화면에 그대로 있다

### 🔴 세 컷을 이어서 보여줄 것 — 배지 구성이 질문마다 달라진다

**이게 라우팅이 실제로 도는 유일한 가시적 증거다.** 고정 순서는 하드코딩된 파이프라인과 화면상 구분이 안 되지만, 질문에 따라 배지 개수와 종류가 바뀌는 건 흉내 낼 수 없다.

```
"…편익이랑 복화 가능성 알려주세요"   배지 5개   물량 · 방향 · 복화 · 환경 · 사회
"복화 가능성만 알려주세요"           배지 2개   방향 · 복화
"왜 이렇게 오래 걸리나요?"           배지 2개   정차시간 · 소요시간
"부산신항에서 오봉으로…"             배지 0개   (도구를 안 부른다)
```

**순서를 짚지 말 것.** #84에서 순서 강제를 걷어냈다 — IDEA.md:44 *"고정 파이프라인이 아니다. 질문에 따라 어떤 함수를 부를지 LLM이 정한다"* 가 근거다. 순서를 자랑하면 오히려 우리 설계를 부정하는 말이 된다.

시간이 짧으면 위 네 줄을 빠르게 연속으로 보여주는 것만으로도 AI층 설명이 된다.

### 여유가 있으면 — 우측 패널을 끝까지 스크롤

#127로 좌측 내비의 두 화면이 **우측 근거 패널 안의 절**로 옮겨졌다. 답이 온 뒤 패널을 아래로 내리면 나온다:

- **계산 단계** — 도구별 입출력 원문과 상태(`공식 기준값 대기` 같은 표시 포함). "AI가 뭘 했는지"를 심사위원이 직접 확인하는 자리
- **데이터 출처** — 원본 4종 + 조인 금지 이유

### 촬영 전 확인

- [ ] 배포본이 최신 main인지 — Vercel 대시보드 최근 배포 시각
- [ ] 주소가 맞는지 — `rail-console.vercel.app` (주영 계정. 옛 주소 `rail-console-for-romanxe1` 은 배포가 멈춰 있다)
- [ ] **편익 카드에 금액이 뜨는지** — `공식 기준값 대기` 만 뜨면 #121 이 배포에 안 올라간 것이다
- [ ] 배지가 한국어인지 — `B5 복화 가능성` 처럼 영문 코드명이 보이면 #122 미배포
- [ ] 🔴 **첫 화면이 3단 대화 화면인지** — 경고 배너와 KPI 카드가 보이면 **#127 이전 배포본**이다.
      맞는 화면이면 가운데에 `화물을 철도로 옮기면 무엇이 나아질까요?` 와 제안 질문 3개만 뜬다
- [ ] 세 번째 제안 질문(`구미에서 부산으로…`)을 눌렀을 때 **부산진·부산신항 같은 역 후보를 되묻는지** — #127로 들어간 동작이다
- [ ] 근거 카드 라벨이 `물량 (2025 하반기)` 인지 (`연간 물량`이면 #105 미머지)
- [ ] 답변 본문에 `_` 가 들어간 영문 식별자가 없는지 (있으면 #99 미반영)
- [ ] 함수 호출 배지가 한국어인지 (`b5_backhaul` 로 뜨면 #97 미반영)

### 촬영 팁

- **①은 제안 질문 클릭**이라 오타 위험이 없다. ②③은 직접 입력이니 미리 복사해두면 좋다
- 답변까지 **20~30초** 걸린다. 그 사이 "함수 호출 중…"이 뜨는데, 그 대기 자체가 "실제로 계산하고 있다"는 증거라 잘라내지 말 것
- 지도 강조는 답변이 끝난 **직후** 바뀐다 — 그 순간을 놓치지 말 것

## 4. 제출 전 체크리스트

| | 항목 | 상태 |
|---|---|---|
| 링크 | 익명 접속 200 | ✅ 11:54 확인 |
| 링크 | 별칭 도메인 혼동 없음 | ✅ 위 ⚠️ 참고 |
| 코드 | 저장소 public | ✅ |
| 코드 | README 실행법이 실제 스크립트와 일치 | ✅ #63 |
| 화면 | 탭 제목 | ✅ #70 · `철도 전환 편익 오케스트레이터` |
| 화면 | 근거 카드 | ✅ #76 · #105 |
| 화면 | 배지 한국어 라벨 | ✅ #97 |
| 화면 | 답변에 내부 필드명 없음 | ✅ #99 · 브라우저 확인 |
| 화면 | 데이터 창 표기(139일) | ✅ #104 · #105 |
| 화면 | KPI·배너 수치 일치 | ✅ #109 (14:57 머지) |
| 답변 | 데이터 한계 자동 고지 | ✅ #101 · 브라우저 확인 |
| 영상 | 촬영·업로드 | ⏳ 사람 |
| 보고서 | 결과보고서 PPTX 11장 | ⏳ 주영·승빈 |
