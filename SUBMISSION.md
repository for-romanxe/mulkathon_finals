# 제출물 — 철도 전환 편익 오케스트레이터

심사위원에게 내보내는 것 3개. **링크 2개는 확인 완료, 영상은 사람이 찍어야 한다.**

## 1. 접속 링크

<https://rail-console-for-romanxe1.vercel.app>

- 2026-08-13 11:54 익명 접속 확인 (`curl` 로그인 없이 HTTP 200)
- main에 머지되면 자동 배포된다 (#23에서 연결)

⚠️ `https://rail-console.vercel.app` 는 **다른 프로젝트**다. 도메인이 비슷하지만 A1 플레이스홀더를 서빙 중이므로 제출에 쓰지 않는다.

## 2. 소스 코드

<https://github.com/for-romanxe/mulkathon_finals>

- Public 확인 완료 — 로그인 없이 열린다
- 개발 방식 설명은 `HOW-IT-WORKS.md`, 실행법은 `README.md`(#53)

## 3. 시연 영상 — ⚠️ 사람이 할 일

루프는 화면 녹화를 할 수 없다. 아래는 **찍을 것만 정리한 것**이고, 촬영·편집·업로드는 사람이 한다.

### 찍을 장면 — 2컷이면 충분하다

이 제품의 주장은 "숫자는 코드가, 해석은 AI가 — 환각 0"이다. 그 주장은 **답한 장면 하나로는 증명되지 않는다.** 모르는 걸 모른다고 하는 장면이 같이 있어야 증명된다.

**① 긍정 경로** — 도구 4종이 순서대로 불리고 수치가 나온다

```
질문: 오봉에서 북철송장으로 컨테이너 보내는 화주인데, 편익이랑 복화 가능성 알려주세요
```

화면에서 짚을 것:
- 함수 호출 배지 `b3_od_lookup → b4_directional → c1_env_benefit → c2_social_benefit` (4/4)
- 답변 속 수치가 전부 배지의 도구 반환값과 같다는 것 — 37.7만톤 · 1억5,399만 톤km · 408.1km · 역방향 24.8만톤(39.7%)
- 근거 카드 — 어느 데이터에서 나온 숫자인지

**② 거절 경로** — 데이터에 없으면 지어내지 않는다

```
질문: 구미에서 부산으로 컨테이너 주 3회 보내려는데요
```

화면에서 짚을 것:
- `found:false` → "임의로 수치를 추정해 드릴 수 없습니다"
- 그냥 거절이 아니라 **실제 취급역(부산진)으로 다시 물어보라고 제안**하는 것

두 질문 모두 2026-08-13 오전 실제 API 키로 검증된 것이다 (#39 PR 코멘트에 로그).

### 촬영 전 확인

- [ ] `.env` 에 `ANTHROPIC_API_KEY` 가 있는지 — 없으면 `/api/chat` 이 500을 낸다
- [ ] 배포본이 최신 main인지 (Vercel 대시보드 최근 배포 시각)
- [ ] 브라우저 탭 제목 — **#70 미해결이면 `Create Next App` 으로 찍힌다**
- [ ] 근거 카드 — **#72 미해결이면 `O-D undefined건` 으로 찍힌다**

## 4. 제출 전 체크리스트

| | 항목 | 상태 |
|---|---|---|
| 링크 | 익명 접속 200 | ✅ 11:54 확인 |
| 링크 | 별칭 도메인 혼동 없음 | ✅ 위 ⚠️ 참고 |
| 코드 | 저장소 public | ✅ |
| 코드 | README 실행법이 실제 스크립트와 일치 | ⏳ #53 리뷰 중 |
| 화면 | 탭 제목 | ⏳ #70 |
| 화면 | 근거 카드 건수 | ⏳ #72 |
| 영상 | 촬영·업로드 | ⏳ 사람 |
